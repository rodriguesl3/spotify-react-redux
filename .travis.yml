dist: trusty
sudo: required


language: node_js
node_js:
  - "stable"

script:
  - npm run build

addons:
  sonarcloud:
    organization: "rodriguesl3_spotify-react-redux"
  token:
    secure: ${SONAR_TOKEN}

jdk:
  - oraclejdk8

script:
  - sonar-scanner -X
deploy:
  provider: surge
  skip_cleanup: true
  domain: spotify-react-redux.surge.sh
  project: ./build/
on:
  all_branches: true